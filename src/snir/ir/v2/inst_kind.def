// This macro should be defined before including this header:
// - SNIR_INSTRUCTION(Id, Token)

SNIR_INSTRUCTION(Nop, nop)
SNIR_INSTRUCTION(Const, const)

SNIR_INSTRUCTION(Return, ret)
SNIR_INSTRUCTION(Branch, br)
SNIR_INSTRUCTION(Phi, phi)

SNIR_INSTRUCTION(Add, add)
SNIR_INSTRUCTION(Sub, sub)
SNIR_INSTRUCTION(Mul, mul)
SNIR_INSTRUCTION(Div, div)
SNIR_INSTRUCTION(Mod, mod)
SNIR_INSTRUCTION(And, and)
SNIR_INSTRUCTION(Or, or)
SNIR_INSTRUCTION(Xor, xor)
SNIR_INSTRUCTION(ShiftLeft, shl)
SNIR_INSTRUCTION(ShiftRight, shr)
SNIR_INSTRUCTION(IntCmp, icmp)

SNIR_INSTRUCTION(FloatAdd, fadd)
SNIR_INSTRUCTION(FloatSub, fsub)
SNIR_INSTRUCTION(FloatMul, fmul)
SNIR_INSTRUCTION(FloatDiv, fdiv)

SNIR_INSTRUCTION(Trunc, trunc)
